name: 'Build die-koma.org'
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
jobs:
  build:
    name: 'Build the die-koma.org website'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - uses: nixbuild/nix-quick-install-action@v34
      - env:
          BRANCH_NAME_OR_REF: ${{ github.head_ref || github.ref }}
        run: echo "name=BRANCH_NAME::${BRANCH_NAME_OR_REF#refs/heads/}" >> $GITHUB_ENV
      - run: echo "name=PR_NUMBER::$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")" >> $GITHUB_ENV
      - name: Restore and save the Nix store
        uses: nix-community/cache-nix-action@v7
        with:
          primary-key: die-koma.org-build-${{ runner.os }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
          restore-prefixes-first-match: die-koma.org-build-${{ runner.os }}-
          gc-max-store-size-linux: 2.5G
          purge: true
          purge-prefixes: die-koma.org-build-${{ runner.os }}-
          purge-created: 0
          purge-last-accessed: 0
          purge-primary-key: never
      - run: nix --print-build-logs build .#KoMaHomepageTar -o homepage.tar.xz
      - uses: actions/upload-artifact@v6
        with:
          name: build-artifacts
          path: homepage.tar.xz
      - name: Build and collect garbage
        run: |
          nix build .\#KoMaHomepageTar .\#devShells.x86_64-linux.default
          nix-store --gc --option keep-derivations true --option keep-outputs true

  check-links:
    name: 'Check for broken links'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: nixbuild/nix-quick-install-action@v34
      - env:
          BRANCH_NAME_OR_REF: ${{ github.head_ref || github.ref }}
        run: echo "name=BRANCH_NAME::${BRANCH_NAME_OR_REF#refs/heads/}" >> $GITHUB_ENV
      - run: echo "name=PR_NUMBER::$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")" >> $GITHUB_ENV
      - name: Restore and save the Nix store
        uses: nix-community/cache-nix-action@v7
        with:
          primary-key: die-koma.org-check-links-${{ runner.os }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
          restore-prefixes-first-match: die-koma.org-check-links-${{ runner.os }}-
          gc-max-store-size-linux: 2.5G
          purge: true
          purge-prefixes: die-koma.org-check-links-${{ runner.os }}-
          purge-created: 0
          purge-last-accessed: 0
          purge-primary-key: never
      - env:
          BRANCH_NAME_OR_REF: ${{ github.head_ref || github.ref }}
        run: echo "name=BRANCH_NAME::${BRANCH_NAME_OR_REF#refs/heads/}" >> $GITHUB_ENV
      - run: echo "name=PR_NUMBER::$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")" >> $GITHUB_ENV
      - run: nix --print-build-logs run .#check-links
      - name: Build and collect garbage
        run: |
          nix build .\#KoMaHomepage
          nix-store --gc --option keep-derivations true --option keep-outputs true

  release:
    if: github.repository == 'Die-KoMa/die-koma.org' && github.event_name == 'push' && github.ref == 'refs/heads/main'
    name: 'Release the website to the release branch'
    concurrency:
      # Allow one concurrent deployment
      group: 'release'
      cancel-in-progress: true
    permissions:
      contents: write
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: release
      - uses: actions/download-artifact@v7
        with:
          name: build-artifacts
      - run: rm -rf homepage
      - run: tar xvf homepage.tar*
      - run: mv dist homepage
      - run: git add homepage
      - run: git status
      - run: git config --global user.email "homepage@die-koma.org"
      - run: git config --global user.name "KoMa GitHub Runner"
      - run: git commit -m "Update homepage built from $GITHUB_SHA" || true
      - run: git push
